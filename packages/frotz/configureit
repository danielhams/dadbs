#!/usr/bin/env mksh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1

# Early exit for diffing.
#exit 1

# No "configure" for frotz - we'll override the Makefile env vars in build
#$SHELL ./configure --prefix=$INSTALLDIR || exit -1

exit 0
