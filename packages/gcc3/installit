#!/usr/bin/env sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
# Only C for now
make CC="$CC" CFLAGS="$CFLAGS" CXXFLAGS="$CXXFLAGS" LIBGCC2_DEBUG_CFLAGS="$LIBGCC2_DEBUG_CFLAGS" MAKEINFO=true CONFIG_SHELL="$CONFIG_SHELL" SHELL="$SHELL" LANGUAGES="c" install || exit -1

# Exit for manual hash updating ("stop the world rebuild")
#echo "Installation successful - update hashes."
#exit 1

exit 0
