#!/usr/bin/env sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1

# Early exit for patch building
#exit 1

$SHELL ./configure --prefix=$INSTALLDIR --without-gnu-malloc --enable-readline --disable-nls || exit -1
exit 0
