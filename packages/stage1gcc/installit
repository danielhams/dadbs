#!/usr/bin/env sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
make CC="$CC" CFLAGS="$CFLAGS" CXXFLAGS="$CXXFLAGS" MAKEINFO=true CONFIG_SHELL="$CONFIG_SHELL" SHELL="$SHELL" LANGUAGES="c c++" install || exit -1

# Exit for manual hash updating ("stop the world rebuild")
#echo "Installation successful - update hashes."
#exit 1

exit 0
