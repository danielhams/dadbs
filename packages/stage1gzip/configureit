#!/usr/bin/env mksh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1

# Early exit for diffing.
#exit 1

# Work around not have coreutils installed yet.
export ACCEPT_INFERIOR_RM_PROGRAM=yes
$SHELL ./configure --prefix=$INSTALLDIR || exit -1

exit 0
