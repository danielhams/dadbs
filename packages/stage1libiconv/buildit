#!/usr/bin/env sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
cd ../build-libiconv
make SHELL="$SHELL" CONFIG_SHELL="$CONFIG_SHELL" || exit -1
exit 0
