diff -Naur libiconv-1.14/libcharset/lib/localcharset.c libiconv-1.14-patched/libcharset/lib/localcharset.c
--- libiconv-1.14/libcharset/lib/localcharset.c	2011-02-28 22:43:35.000000000 +0000
+++ libiconv-1.14-patched/libcharset/lib/localcharset.c	2026-07-17 15:12:55.800325000 +0000
@@ -124,7 +124,7 @@
   cp = charset_aliases;
   if (cp == NULL)
     {
-#if !(defined DARWIN7 || defined VMS || defined WIN32_NATIVE || defined __CYGWIN__)
+#if !(defined DARWIN7 || defined VMS || defined WIN32_NATIVE || defined __CYGWIN__ || defined AMIX)
       const char *dir;
       const char *base = "charset.alias";
       char *file_name;
@@ -251,6 +251,15 @@
 
 #else
 
+# if defined AMIX
+      /* Hardcode AMIX/SVR4 aliases. 
+         Format: ALIAS \0 CANONICAL \0 ... \0 \0 */
+      cp = "USASCII" "\0" "ASCII" "\0"
+           "ISO"     "\0" "ISO-8859-1" "\0"
+           "C"       "\0" "ASCII" "\0"
+           "*"       "\0" "ASCII" "\0";
+# endif
+
 # if defined DARWIN7
       /* To avoid the trouble of installing a file that is shared by many
          GNU packages -- many packaging systems have problems with this --,
diff -Naur libiconv-1.14/srclib/unistd.in.h libiconv-1.14-patched/srclib/unistd.in.h
--- libiconv-1.14/srclib/unistd.in.h	2011-08-07 13:42:06.000000000 +0000
+++ libiconv-1.14-patched/srclib/unistd.in.h	2026-07-10 05:20:56.000000000 +0000
@@ -1155,11 +1155,20 @@
 #  if !(defined __cplusplus && defined GNULIB_NAMESPACE)
 #   define readlink rpl_readlink
 #  endif
+#if defined(AMIX)
+_GL_FUNCDECL_RPL (readlink, int,
+                  (const char *file, char *buf, size_t bufsize)
+                  _GL_ARG_NONNULL ((1, 2)));
+_GL_CXXALIAS_RPL (readlink, int,
+                  (const char *file, char *buf, size_t bufsize));
+#else
 _GL_FUNCDECL_RPL (readlink, ssize_t,
                   (const char *file, char *buf, size_t bufsize)
                   _GL_ARG_NONNULL ((1, 2)));
 _GL_CXXALIAS_RPL (readlink, ssize_t,
                   (const char *file, char *buf, size_t bufsize));
+#endif
+
 # else
 #  if !@HAVE_READLINK@
 _GL_FUNCDECL_SYS (readlink, ssize_t,
