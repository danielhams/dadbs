#!/usr/gnutc/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
make LDDLFLAGS="-shared" LIBS="-lsocket -nsl -lc -lcrypt -lx -ldl /usr/ccs/lib/libm.a" install || exit -1

exit 0
